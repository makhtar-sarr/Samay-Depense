/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ShowPipe } from './show.pipe';

describe('Pipe: Showe', () => {
  it('create an instance', () => {
    let pipe = new ShowPipe();
    expect(pipe).toBeTruthy();
  });
});
